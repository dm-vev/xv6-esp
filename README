xv6 is a re-implementation of Dennis Ritchie's and Ken Thompson's Unix
Version 6 (v6).  xv6 loosely follows the structure and style of v6,
but is implemented for a modern RISC-V multiprocessor using ANSI C.

ACKNOWLEDGMENTS

xv6 is inspired by John Lions's Commentary on UNIX 6th Edition (Peer
to Peer Communications; ISBN: 1-57398-013-7; 1st edition (June 14,
2000)).  See also https://pdos.csail.mit.edu/6.1810/, which provides
pointers to on-line resources for v6.

The following people have made contributions: Russ Cox (context switching,
locking), Cliff Frey (MP), Xiao Yu (MP), Nickolai Zeldovich, and Austin
Clements.

We are also grateful for the bug reports and patches contributed by
Abhinavpatel00, Takahiro Aoyagi, Marcelo Arroyo, Hirbod Behnam, Silas
Boyd-Wickizer, Anton Burtsev, carlclone, Ian Chen, clivezeng, Dan
Cross, Cody Cutler, Mike CAT, Tej Chajed, Asami Doi,Wenyang Duan,
echtwerner, eyalz800, Nelson Elhage, Saar Ettinger, Alice Ferrazzi,
Nathaniel Filardo, flespark, Peter Froehlich, Yakir Goaron, Shivam
Handa, Matt Harvey, Bryan Henry, jaichenhengjie, Jim Huang, Matúš
Jókay, John Jolly, Alexander Kapshuk, Anders Kaseorg, kehao95,
Wolfgang Keller, Jungwoo Kim, Jonathan Kimmitt, Eddie Kohler, Vadim
Kolontsov, Austin Liew, l0stman, Pavan Maddamsetti, Imbar Marinescu,
Yandong Mao, Matan Shabtay, Hitoshi Mitake, Carmi Merimovich,
mes900903, Mark Morrissey, mtasm, Joel Nider, Hayato Ohhashi,
OptimisticSide, papparapa, phosphagos, Harry Porter, Greg Price, Zheng
qhuo, Quancheng, RayAndrew, Jude Rich, segfault, Ayan Shafqat, Eldar
Sehayek, Yongming Shen, Fumiya Shigemitsu, snoire, Taojie, Cam Tenny,
tyfkda, Warren Toomey, Stephen Tu, Alissa Tung, Rafael Ubal, unicornx,
Amane Uehara, Pablo Ventura, Luc Videau, Xi Wang, WaheedHafez, Keiichi
Watanabe, Lucas Wolf, Nicolas Wolovick, wxdao, Grant Wu, x653, Andy
Zhang, Jindong Zhang, Icenowy Zheng, ZhUyU1997, and Zou Chang Wei.

ERROR REPORTS

Please send errors and suggestions to Frans Kaashoek and Robert Morris
(kaashoek,rtm@mit.edu).  The main purpose of xv6 is as a teaching
operating system for MIT's 6.1810, so we are more interested in
simplifications and clarifications than new features.

BUILDING AND RUNNING XV6

You will need a RISC-V "newlib" tool chain from
https://github.com/riscv/riscv-gnu-toolchain, and qemu compiled for
riscv64-softmmu.  Once they are installed, and in your shell
search path, you can run "make qemu".

ESP32-S3 (ESP-IDF 6.0) M1 PORT

This repository also contains a first ESP-IDF based bring-up path for
ESP32-S3 (`main/app_main.c` with code in `kernel/*_esp*.c`).

Key properties of this M1 bring-up:
- single app task runtime (`app_main`),
- UART interactive kernel shell (`ksh`),
- raw SPI flash partition block backend (`xv6fs`).

Build steps:
1. Install ESP-IDF 6.0 and export the environment.
2. Set target: `idf.py set-target esp32s3`
3. Build: `idf.py build`
4. Flash and monitor: `idf.py -p <PORT> flash monitor`

Filesystem image source:
- Everything under `fsroot/` is packed into `xv6fs.bin` during build.
- ELF user commands are described as applets via `applets/<name>/applet.cmake`.
- Each applet manifest defines compile sources, install path, deps/resources, and enabled state.
- Enabled applets are built into `build/applets/*` and staged into `build/fsroot_stage/*` before `mkfs`.

Applet layout:
- shared headers: `applets/include/`
- one applet per directory: `applets/<name>/`
- manifest path: `applets/<name>/applet.cmake`

Applet manifest example:
```cmake
xv6_applet(
  NAME ls
  ENABLED ON
  SOURCES ls.c
  BUILD_DEPS ../include/xv6_user.h
  INSTALL_PATH /bin/ls
)
```

BSD-oriented applets currently included:
`basename`, `cat`, `cp`, `dd`, `dirname`, `echo`, `false`, `head`, `ls`, `mkdir`, `mv`, `pwd`, `rm`, `sleep`, `touch`, `true`, `uname`.

Project-specific extra applets:
`mem`, `ticks`, `write`, `ptydemo`, `ptysend`, `ptyrecv`, `stdinhead`, `hostabi_probe`.

ESP QEMU smoke test:
- `./scripts/qemu_smoke_esp.py`
- `idf.py qemu_smoke`
- `./scripts/qemu_stress_esp.py`
- `idf.py qemu_stress`
- `./scripts/qemu_soak_esp.py`
- `idf.py qemu_soak`
- `./scripts/qemu_applets_esp.py`
- `idf.py qemu_applets`
- `./scripts/qemu_regressions_esp.py`
- `idf.py qemu_regress`
- `./scripts/hil_smoke_esp.py --port /dev/ttyACM0`
- `idf.py hil_smoke`
- `./scripts/hil_applets_esp.py --port /dev/ttyACM0`
- `idf.py hil_applets`
- `./scripts/hil_gate_esp.py --port /dev/ttyACM0 --cycles 3`
- `idf.py hil_gate`

CI gates (mandatory):
- local unified gate runner: `./scripts/ci_gate_esp.py`
- CMake target: `idf.py ci_gate`
- workflow: `.github/workflows/ci-gates.yml`
- enforced sequence: `build + static_analysis + qemu_smoke + qemu_applets + qemu_soak + qemu_stress + qemu_regressions`
- triage artifacts in CI: `build/triage/*` (uploaded from workflow)

RISC-V CI path:
- workflow: `.github/workflows/riscv-ci.yml`
- enforced sequence: `make kernel/kernel fs.img` + `test-xv6.py -q usertests` + `test-xv6.py crash`

Hardware-in-the-loop CI path:
- workflow: `.github/workflows/hil-gate.yml`
- runner requirement: self-hosted label set `linux`, `esp32-hil`
- trigger: manual (`workflow_dispatch`) and weekly scheduled run

Crash triage / fail-fast:
- wrapper: `./scripts/crash_triage_runner.py --name qemu_smoke -- python3 ./scripts/qemu_smoke_esp.py`
- artifacts: `build/triage/<timestamp>-<step>/output.log` + `result.json`

Host ABI snapshot freeze:
- check: `./scripts/check_hostabi_abi.py`
- update snapshot intentionally: `./scripts/check_hostabi_abi.py --update`
- snapshot file: `docs/abi/hostabi_exports.snapshot`

Static analysis:
- unified local runner: `./scripts/static_analysis.sh`
- default mode is fail-closed (`STRICT=1`)
- optional relaxed local mode: `STRICT=0 BUILD_DIR=build ./scripts/static_analysis.sh`
- CMake target: `idf.py static_analysis`
- CI workflow: `.github/workflows/static-analysis.yml`
- pre-commit integration: `pre-commit install` (runs static pipeline before commit)

Static toolset currently integrated:
- `cppcheck` (C/C++)
- `clang-tidy` (C/C++, via `build/compile_commands.json`)
- `semgrep` (SAST, `--config auto`)
- `ruff` (Python lint)
- `bandit` (Python security checks)
- `codespell` (typo scan)

Includes PTY check via `ptydemo` (`/dev/ptmx` <-> dynamic `/dev/pts/N`).
Also checks two-ELF PTY flow: `ptysend` -> `ptyrecv`.
Shell supports background jobs: `cmd &`, `ps`, `jobs`, `fg <jobid>`, `kill <jobid>`, `wait [jobid]`.
Shell health telemetry builtin: `health` (uptime, heap, job counters, runtime limits).
Pipe-like IPC in shell: `left | right`.
Shell redirection: `<`, `>`, `>>`, `2>`, `2>>`.
Job limits: `limit <ms> <heap_kb> <cmd...> [&]`.

The ESP build is intentionally separate from the original `make qemu`
flow and does not replace the RISC-V path.

Release and operations:
- tag-based release workflow: `.github/workflows/release.yml` (`v*` tags)
- local release bundle command: `python3 ./scripts/release_bundle.py --version vX.Y.Z --commit $(git rev-parse HEAD)`
- security disclosure policy: `SECURITY.md`
- runbooks and operational docs:
  - `docs/configuration.md`
  - `docs/operations/release.md`
  - `docs/operations/incident-response.md`
